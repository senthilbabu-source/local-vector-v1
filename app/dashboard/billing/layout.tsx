// P6-FIX-27: Page title for client component billing page
export const metadata = { title: 'Billing | LocalVector.ai' };

export default function BillingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
