// /signup is an alias for /register.
// Both routes render the same registration form so that marketing links to
// either URL reach the same page without a client-side redirect.
export { default } from '@/app/(auth)/register/page';
